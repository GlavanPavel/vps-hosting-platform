[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    e2e: end-to-end tests that drive the live stack (API + Celery + OpenStack); skipped if the API is unreachable
